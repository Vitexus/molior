# TODO: Debian 14 Forky - Review Python version requirements and dependencies
from setuptools import setup, find_packages

setup(name="molior", install_requires=[], packages=find_packages(exclude=("tests",)))
